<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <!-- Aquí debes elegir cuál de los siguientes estilos 'NormalTheme' deseas utilizar y eliminar los demás. -->

    <!-- Ejemplo de un estilo 'NormalTheme' -->
    <style name="NormalTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Atributos del estilo aquí -->
    </style>

    <!-- Asegúrate de eliminar o comentar las definiciones de estilo adicionales que no estés utilizando. -->
</resources>
